{
  "name": "@oda/etl",
  "version": "1.0.0",
  "description": "ETL service for processing scraped JSON data and persisting to PostgreSQL.",
  "private": true,
  "scripts": {
    "start": "ts-node src/main.ts",
    "build": "tsc",
    "etl:grupo": "ts-node src/main.ts grupo",
    "etl:pesquisador": "ts-node src/main.ts pesquisador",
    "etl:all": "ts-node src/run-all.ts",
    "queue:prepare": "pnpm --filter @oda/etl... run build --incremental false",
    "queue:etl": "node dist/queue/etlCli.js",
    "test:queue": "pnpm run build && node --test tests/*.test.cjs",
    "export:openalex-areas": "ts-node src/exportOpenAlexAreas.ts",
    "import:openalex-areas": "ts-node src/exportOpenAlexAreas.ts --import",
    "relate:openalex-areas": "ts-node src/relateOpenAlexAreas.ts",
    "test:openalex-areas": "node -r ts-node/register --test src/openAlexAreas.test.ts"
  },
  "dependencies": {
    "chokidar": "^4.0.3",
    "dotenv": "^16.4.7",
    "@oda/database": "workspace:*",
    "@oda/queue": "workspace:*",
    "@oda/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@types/node": "^24.0.0",
    "ts-node": "^10.9.2"
  }
}
